graphhopper:
  datareader.file: "C:\\Users\\danie\\OneDrive\\Desktop\\Training Line Prints\\Route-Viewer\\graphhopper\\pbf\\florida-latest.osm.pbf"
  graph.location:  "./graphs/FL"
  graph.encoded_values: road_class,osm_way_id
  import.osm.ignored_highways: motorway,trunk
  profiles:
    - name: foot
      encoder: foot
      weighting: custom
      custom_model:
        speed:
          - if: "true"
            limit_to: 5
    - name: bike
      encoder: bike
      weighting: custom
      custom_model:
        speed:
          - if: "true"
            limit_to: 18
    - name: car
      encoder: car
      weighting: custom
      custom_model:
        speed:
          - if: "true"
            limit_to: 100

server:
  application_connectors:
    - type: http
      port: 8986


